# Legal Graph RAG — runtime dependencies
#
# Versions are the ones this project is verified against. A fresh clone should
# be able to run:
#     pip install -r requirements.txt
#     python graph_agent.py
# with nothing else needed except the gcloud CLI (for ADC auth).

# ─── LLM & Inference (Gemini via Vertex AI) ────────────────────
# ChatVertexAI is deprecated in >=4.0 in favour of
# ChatGoogleGenerativeAI(vertexai=True); we deliberately stay on the
# ChatVertexAI path for now — see the DEPENDENCY NOTE in agents/llm.py.
# The <4.0.0 pin is load-bearing; do not relax it without reading that note.
langchain-google-vertexai>=3.2,<4.0.0
google-cloud-aiplatform>=1.157
google-auth>=2.53

# ─── LangChain Ecosystem ───────────────────────────────────────
langchain>=1.3
langchain-core>=1.4
langchain-community>=0.4
langgraph>=1.2
langsmith>=0.8

# ─── Graph Database ────────────────────────────────────────────
neo4j>=6.2

# ─── Config & Validation ───────────────────────────────────────
# config.py is built on pydantic-settings; without it nothing imports.
pydantic>=2.13
pydantic-settings>=2.14

# ─── PDF Parsing (ingest/ only — not needed at query time) ──────
pdfplumber>=0.11
pypdf>=6.12

# ─── Data Handling (ingest/ only) ──────────────────────────────
pandas>=2.3
jsonlines>=4.0

# ─── API Runtime ───────────────────────────────────────────────
fastapi>=0.136
uvicorn[standard]>=0.49

# ─── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.2
tenacity>=9.1
tqdm>=4.67

# ─── Scraping (v2 — ruling ingest, not used at runtime) ─────────
requests>=2.34
beautifulsoup4>=4.14
